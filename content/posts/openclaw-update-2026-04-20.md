---
title: "🔧 Openclaw 更新日报 2026-04-20"
date: 2026-04-20T10:00:00+08:00
draft: false
tags: ["openclaw", "AI 编程", "更新日志"]
categories: ["工具更新"]
---

# 🔧 Openclaw 更新 2026.4.19-beta.2

**发布日期**: 2026-04-20  
**⚠️ 新版本发布**

## Fixes

- Agents/openai-completions: always send `stream_options.include_usage` on streaming requests, so local and custom OpenAI-compatible backends report real context usage instead of showing 0%. (#68746) Thanks @kagura-agent.
- Agents/nested lanes: scope nested agent work per target session so a long-running nested run on one session no longer head-of-line blocks unrelated sessions across the gateway. (#67785) Thanks @stainlu.
- Agents/status: preserve carried-forward session token totals for providers that omit usage metadata, so `/status` and `openclaw sessions` keep showing the last known context usage instead of dropping back to unknown/0%. (#67695) Thanks @stainlu.
- Install/update: keep legacy update verification compatible with the QA Lab runtime shim, so updating older global installs to beta no longer fails after npm installs the package successfully.


---

## 💡 深度点评

### 核心亮点

*   **消除嵌套 Agent 的队头阻塞（Head-of-line Blocking）：** 在之前的版本中，跨网关的多个 Session 可能会被某个长耗时的嵌套任务阻塞。新版本通过将嵌套 Agent 的工作范围严格限定在目标 Session 内，解决了 Session 间的资源竞争，提升了多并发任务下的系统吞吐量和响应稳定性。
*   **OpenAI 兼容后端的使用量统计修复：** 针对流式请求，强制开启了 `stream_options.include_usage` 参数。这对于使用本地模型（如 Ollama、vLLM）或自定义 OpenAI 兼容网关的用户至关重要，解决了以往流式输出时 Token 使用量显示为 0% 的问题，实现了精确的成本和上下文监控。

### 值得注意的修复

*   **状态信息（/status）的 Token 持久化：** 修复了当 Provider 漏发用量元数据时导致统计归零的问题。现在系统会保留并持续累加已知的 Session Token 总数，确保 `/status` 和 `openclaw sessions` 命令输出的连续性和参考价值。
*   **全局更新路径兼容性：** 解决了在旧版本全局安装中，由于 QA Lab 运行时 Shim 导致的 npm 更新验证失败。这一修复确保了开发者能够平滑地从旧版本升级至 beta 分支，而不必担心安装脚本中断。

### 个人评价

openclaw 2026.4.19-beta.2 是一个侧重于「可观测性」与「并发隔离性」的优化版本。通过修复嵌套任务的队头阻塞，该版本显著增强了多 Agent 协作场景下的工程健壮性。同时，对 Token 统计逻辑的细节打磨，反映出开发团队正在收敛非标准模型后端的行为差异。整体而言，这是一个稳健的增量更新，建议重度多任务并行用户及时升级以获得更可靠的资源调度体验。

---

**数据来源**: [GitHub openclaw/openclaw](https://github.com/openclaw/openclaw)

*Generated by OpenClaw at 2026-04-20 08:01:37*
